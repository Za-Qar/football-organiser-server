package com.football.organiser.server.services;

import com.football.organiser.server.models.Game;

public class GameServiceImpl implements GameService {
    @Override
    public boolean createGame(Game game) {
        return false;
    }
}
